# mfish-nocode-view 前端项目设计文档

## 文档信息

| 项目名称 | mfish-nocode-view |
|---------|-------------------|
| 版本号 | 2.3.0 |
| 编写日期 | 2026-04-14 |
| 许可证 | Apache-2.0 |
| 仓库地址 | https://github.com/mfish-qf/mfish-nocode-view |

---

## 1. 项目概述

### 1.1 项目简介

mfish-nocode-view 是一个基于 Vue 3 + TypeScript 的企业级前端解决方案，专注于大屏可视化配置与零代码业务系统构建。该项目采用 Monorepo 架构，通过 `@mfish/core`、`@mfish/stores`、`@mfish/nocode` 等多个子包实现功能模块的解耦与复用。项目集成 Ant Design Vue、Naive UI、ECharts 等主流 UI 库，提供丰富的业务组件与可视化配置能力，支持多租户、OAuth2 统一认证、动态路由、权限控制等企业级特性。

### 1.2 核心能力

- **大屏可视化配置**：基于 `@mfish/nocode` 包提供 50+ 大屏图表组件，支持拖拽式配置、数据绑定、事件交互
- **零代码业务开发**：通过可视化表单、表格、树形等通用组件降低业务开发成本
- **动态路由系统**：基于后端返回的菜单权限动态生成路由，支持菜单、目录、按钮三级权限控制
- **多租户支持**：支持租户隔离、租户切换、租户管理
- **统一认证体系**：支持密码登录、OAuth2 授权码模式、Gitee/Github 第三方登录

### 1.3 技术栈概览

| 类别 | 技术选型 | 版本要求 |
|-----|---------|---------|
| 核心框架 | Vue 3 | ^3.5.29 |
| 类型系统 | TypeScript | catalog |
| 构建工具 | Vite | ^7.3.1 |
| 状态管理 | Pinia | ^3.0.4 |
| 路由管理 | Vue Router | catalog |
| UI 组件库 | Ant Design Vue + Naive UI | catalog |
| 图表库 | ECharts | ^6.0.0 |
| 流程图 | @logicflow/core | catalog |
| 包管理器 | pnpm | >=9 |
| Node 版本 | Node | >=20 |

---

## 2. 架构设计

### 2.1 Monorepo 架构

项目采用 Monorepo 架构，使用 pnpm workspace + turbo 管理多包依赖，结构如下：

```
mfish-nocode-view/
├── packages/
│   ├── core/           # 核心业务包，提供基础组件、hooks、工具函数
│   ├── stores/         # 状态管理包，提供 Pinia store 定义
│   ├── nocode/         # 可视化大屏包，提供图表组件配置
│   └── custom-api/     # 自定义 API 包
├── internal/           # 内部工具配置包
│   ├── eslint-config/  # ESLint 配置
│   ├── prettier-config/# Prettier 配置
│   ├── stylelint-config/# Stylelint 配置
│   ├── ts-config/      # TypeScript 配置
│   └── vite-config/    # Vite 配置
└── src/                # 主应用源码
```

**包依赖关系**：
- `@mfish/core` 被主应用 `@mfish/stores` `@mfish/nocode` 依赖
- `@mfish/stores` 依赖 `@mfish/core`
- 主应用依赖所有 packages

### 2.2 模块职责

| 包名 | 职责 | 核心导出 |
|-----|------|---------|
| `@mfish/core` | 基础组件、hooks、工具函数、路由、权限、国际化、HTTP 客户端 | `api`、`router`、`hooks`、`components/*`、`utils` |
| `@mfish/stores` | Pinia 状态管理定义 | `useUserStore`、`useAppStore`、`usePermissionStore` |
| `@mfish/nocode` | 大屏图表组件配置 | `MfBarLineConfig`、`MfPieConfig` 等 50+ 组件配置 |
| `@mfish/vite-config` | Vite 构建配置 | `createViteConfig` |

### 2.3 核心设计模式

#### 2.3.1 依赖注入模式

通过 `provide/inject` 实现跨层级组件通信：

```typescript
// hooks/UseAppContext.ts
export function useAppContext(): ProjectConfig {
  return inject(APP_CONTEXT_KEY, {} as ProjectConfig);
}
```

#### 2.3.2 组合式函数模式

所有业务 hooks 遵循组合式函数规范，统一导出至 `@mfish/core/hooks`：

```typescript
// hooks/web/UsePermission.ts
export function usePermission() { ... }
```

#### 2.3.3 工厂模式

对于需要动态创建的组件（如 Modal、Drawer），采用工厂模式：

```typescript
// components/Modal/src/hooks/UseModal.ts
export function useModal(config: ModalProps) {
  return createModal(config);
}
```

---

## 3. 技术选型

### 3.1 核心框架

| 技术 | 选型理由 |
|-----|---------|
| Vue 3.5.29 | 组合式 API（Composition API）支持更好的逻辑复用，`<script setup>` 语法糖提升开发体验 |
| TypeScript | 静态类型检查提升代码质量，IDE 支持完善，重构安全性高 |
| Vite 7.3.1 | 基于 ESM 的 HMR 速度快，Rollup 打包优化，插件生态成熟 |

### 3.2 UI 组件库

采用双组件库策略：

| 组件库 | 用途 | 场景 |
|-------|------|-----|
| Ant Design Vue | 主要业务组件 | 表格、表单、弹窗、菜单 |
| Naive UI | 主题配置、配置面板 | 主题切换、设置面板 |

### 3.3 状态管理

| 方案 | 选型理由 |
|-----|---------|
| Pinia ^3.0.4 | Vue 3 官方推荐，TypeScript 支持好，无需 mutation，支持 Devtools |

### 3.4 HTTP 客户端

| 技术 | 特性 |
|-----|------|
| Axios | 请求拦截/响应拦截、自动转换、请求取消、错误处理 |

### 3.5 数据可视化

| 技术 | 用途 |
|-----|------|
| ECharts ^6.0.0 | 大屏图表（柱状图、折线图、饼图、地图等） |
| ECharts-GL | 3D 图表支持 |
| @logicflow/core | 流程图绘制 |

### 3.6 构建与部署

| 技术 | 用途 |
|-----|------|
| turbo | Monorepo 构建编排 |
| vite-plugin-html | HTML 模板处理 |
| vite-plugin-mock | 开发环境 Mock 数据 |
| vite-plugin-svg-icons | SVG 图标优化 |

---

## 4. UI/UX 设计规范

### 4.1 设计系统

项目采用类 Ant Design 设计系统规范：

#### 4.1.1 主题色

默认主题色：`#EE4F12`（橙红色）

预设主题色列表（供用户选择）：

```typescript
const APP_PRESET_COLOR_LIST = [
  "#EE4F12", "#0960BD", "#0084F4", "#009688", "#536DFE",
  "#FF5C93", "#0096C7", "#9C27B0", "#D2AA08", "#FF9800"
  // ... 更多颜色
];
```

#### 4.1.2 暗色模式

支持亮色/暗色主题切换，通过 `ThemeEnum` 枚举控制：

```typescript
enum ThemeEnum {
  LIGHT = 'light',
  DARK = 'dark'
}
```

#### 4.1.3 特殊效果

| 效果 | 配置项 | 说明 |
|-----|-------|-----|
| 灰色模式 | `grayMode` | 用于哀悼日等特殊场景 |
| 色弱模式 | `colorWeak` | 提升色弱用户可访问性 |

### 4.2 布局规范

#### 4.2.1 页面布局

```
┌─────────────────────────────────────────┐
│                 Header                   │
├────────┬────────────────────────────────┤
│        │  MultiTabs                      │
│  Sider ├────────────────────────────────┤
│        │                                │
│        │         Content                │
│        │                                │
├────────┴────────────────────────────────┤
│                 Footer                   │
└─────────────────────────────────────────┘
```

#### 4.2.2 布局配置

```typescript
menuSetting: {
  mode: MenuModeEnum.INLINE,    // 菜单模式：inline/horizontal/mix
  type: MenuTypeEnum.SIDEBAR,   // 菜单类型：sidebar/top/mix
  fixed: true,                  // 固定菜单
  collapsed: false,             // 默认展开
  menuWidth: 210,              // 菜单宽度
  theme: ThemeEnum.LIGHT,       // 菜单主题
  accordion: true               // 手风琴模式
}
```

### 4.3 组件规范

#### 4.3.1 基础组件

| 组件 | 说明 |
|-----|------|
| Basic | 最底层基础组件 |
| Button | 按钮组件，支持 ripple 效果 |
| Form | 表单组件封装 |
| Table | 表格组件封装 |
| Modal/Drawer | 弹窗/抽屉组件封装 |
| Upload | 文件上传组件 |
| Tree | 树形组件 |

#### 4.3.2 业务组件

| 组件 | 说明 |
|-----|------|
| DictTag | 字典标签组件 |
| CodeEditor | 代码编辑器 |
| Cropper | 图片裁剪 |
| CountDown/CountTo | 倒计时/数字滚动 |
| Draggable | 拖拽排序 |

### 4.4 图标规范

采用 SVG 图标方案：

- 自定义图标通过 `iconfont` 方式引入（`packages/core/src/assets/svg/`）
- 第三方图标库使用 `@iconify/vue`

```typescript
// 使用示例
import { Icon } from '@iconify/vue';
<Icon icon="mdi:home" />
```

---

## 5. 组件设计

### 5.1 组件分层

```
┌─────────────────────────────────────┐
│      页面组件 (Views)               │
│   src/views/**/*.vue                │
├─────────────────────────────────────┤
│      业务组件 (Business)            │
│   src/components/nocode/**          │
├─────────────────────────────────────┤
│      通用组件 (Common)               │
│   src/components/general/**         │
├─────────────────────────────────────┤
│      基础组件 (Basic)                │
│   @mfish/core/components/Basic/**    │
└─────────────────────────────────────┘
```

### 5.2 组件注册

#### 5.2.1 全局注册

通过 `RegisterGlobComponent.ts` 实现全局组件注册：

```typescript
// src/components/RegisterGlobComponent.ts
export function registerGlobComp(app: App) {
  const glob = import.meta.glob('./general/**/*.vue');
  for (const [key, component] of Object.entries(glob)) {
    const name = key.replace('./general/', '').replace('.vue', '');
    app.component(name, component);
  }
}
```

#### 5.2.2 按需引入

业务组件采用按需引入方式，组件文件包含 `index.ts` 统一导出：

```typescript
// packages/core/src/components/Modal/index.ts
export { default as Modal } from './src/Modal';
export { useModal } from './src/hooks/UseModal';
```

### 5.3 核心组件设计

#### 5.3.1 Modal 组件

采用 Hooks 模式设计，支持配置化创建：

```typescript
// 使用方式
const [ModalRef, { openModal, closeModal }] = useModal();
openModal({ title: '标题', ... });
```

**特性**：
- 支持全屏切换
- 支持拖拽移动
- 支持统一控制（`Modal.destroyAll()`）
- 预设多种尺寸：`xs/sm/md/lg/xl/xxl`

#### 5.3.2 Table 组件

封装 Ant Design Table，提供增强功能：

- 内置分页支持
- 统一 loading 状态
- 列配置序列化
- 行选择支持

#### 5.3.3 Form 组件

封装 Ant Design Form，支持：

- 动态表单项生成
- 自动布局适配
- 校验规则统一管理

### 5.4 大屏组件

`@mfish/nocode` 包提供 50+ 大屏可视化组件：

| 类别 | 组件 |
|-----|------|
| 柱状图/折线图 | `MfBarLineConfig`、`MfBarLineDataConfig` |
| 饼图 | `MfPieConfig`、`MfPieDataConfig` |
| 地图 | `MfMapConfig`、`MfMapChunksConfig` |
| 表格 | `MfTableConfig`、`MfScrollTableConfig` |
| 通用 | `MfButtonConfig`、`MfInputConfig`、`MfSelectConfig` |

**组件配置结构**：

```typescript
{
  id: string,
  type: string,           // 组件类型
  name: string,            // 组件名称
  dataConfig: {            // 数据配置
    dataType: 'static' | 'dynamic',
    api?: string,
    params?: object
  },
  styleConfig: {          // 样式配置
    width: number,
    height: number,
    position: { x, y }
  },
  eventConfig: []          // 事件配置
}
```

---

## 6. 状态管理方案

### 6.1 Store 架构

使用 Pinia 进行状态管理，按模块划分：

```
@mfish/stores/src/modules/
├── User.ts       # 用户状态（用户信息、Token、角色）
├── App.ts        # 应用状态（配置、加载状态）
├── Permission.ts # 权限状态（菜单、权限码、动态路由）
├── MultipleTab.ts# 多标签页状态
├── Dict.ts       # 字典状态
├── TableSetting.ts# 表格配置状态
├── I18n.ts       # 国际化状态
└── ErrorLog.ts   # 错误日志
```

### 6.2 核心 Store 设计

#### 6.2.1 UserStore

```typescript
interface UserState {
  userInfo: Nullable<SsoUser>;
  tenantId?: string;
  token?: string;
  refreshToken?: string;
  roleInfoList: RoleInfo[];
  roleList: Set<string>;
  isLogout?: boolean;
}
```

**核心方法**：
| 方法 | 功能 |
|-----|------|
| `login(params)` | 用户登录 |
| `logout()` | 用户登出 |
| `getAccountInfo()` | 获取用户信息并初始化权限 |
| `refreshTokenAction()` | 刷新 Token |

#### 6.2.2 AppStore

```typescript
interface AppState {
  pageLoading: boolean;
  projectConfig: ProjectConfig;
  beforeMiniInfo: BeforeMiniState;
}
```

**核心方法**：
| 方法 | 功能 |
|-----|------|
| `setPageLoadingAction(loading)` | 设置页面加载状态 |
| `setProjectConfig(config)` | 更新项目配置 |
| `resetAllState()` | 重置所有状态 |

#### 6.2.3 PermissionStore

```typescript
interface PermissionState {
  permissions: Set<string>;
  isDynamicAddedRoute: boolean;
  lastBuildMenuTime: number;
  menuList: Menu[];
  homePath: string;
}
```

**核心方法**：
| 方法 | 功能 |
|-----|------|
| `buildRoutesAction()` | 构建动态路由 |
| `addRouter(router)` | 添加路由到 Vue Router |

### 6.3 缓存策略

| 缓存内容 | 存储方式 | 说明 |
|---------|---------|------|
| Token | localStorage | 通过 `auth` 模块统一管理 |
| 项目配置 | localStorage | `Persistent` 工具类 |
| 路由缓存 | sessionStorage | 支持页面刷新恢复 |
| 表格列配置 | localStorage | `TableSetting` 模块 |

```typescript
// 缓存键枚举
enum CacheEnum {
  TOKEN_KEY = 'TOKEN',
  REFRESH_TOKEN_KEY = 'REFRESH_TOKEN',
  TENANT_ID_KEY = 'TENANT_ID',
  USER_INFO_KEY = 'USER_INFO',
  PROJ_CFG_KEY = 'PROJ_CFG'
}
```

---

## 7. 路由设计

### 7.1 路由架构

```
┌──────────────────────────────────────────────────────┐
│                    基础路由 (Basic Routes)            │
│  LoginRoute / Oauth2Route / PAGE_NOT_FOUND_ROUTE      │
├──────────────────────────────────────────────────────┤
│                    动态路由 (Dynamic Routes)           │
│  根据后端菜单权限动态生成                              │
├──────────────────────────────────────────────────────┤
│                    前端路由 (Front Routes)            │
│  写在前端的业务路由（如 /dashboard/workbench）         │
└──────────────────────────────────────────────────────┘
```

### 7.2 基础路由定义

```typescript
// packages/core/src/router/routers/Basic.ts
export const basicRoutes = [
  LoginRoute,           // /login
  Oauth2Route,          // /oauth2
  ErrorLoginRoute,      // /error-login
  REDIRECT_ROUTE,       // /redirect
  PAGE_NOT_FOUND_ROUTE, // /:path(.*)*
  GiteeRoute,           // /giteeOauth2
  GithubRoute           // /githubOauth2
];
```

### 7.3 动态路由生成

根据后端返回的菜单树动态生成路由：

```typescript
// packages/stores/src/modules/Permission.ts
async buildRoutesAction() {
  // 1. 从后端获取菜单树
  const menus = await getRoleMenuTree();

  // 2. 递归构建路由和菜单
  buildMenuRoute(menus, menuList, routes);

  // 3. 多级路由扁平化（超过2级自动提取中间层级）
  const route = flatMultiLevelRoutes(routes);

  // 4. 返回生成的路由
  return route;
}
```

### 7.4 路由守卫

```typescript
// packages/core/src/router/guard/index.ts
export function setupRouterGuard(router: Router) {
  createPageGuard(router);           // 页面状态处理
  createPageLoadingGuard(router);     // 页面加载状态
  createHttpGuard(router);            // HTTP 请求管理
  createScrollGuard(router);          // 滚动位置管理
  createMessageGuard(router);         // 消息清理
  createProgressGuard(router);       // 进度条控制
  createPermissionGuard(router);     // 权限校验
  createParamMenuGuard(router);       // 参数化菜单处理
  createStateGuard(router);          // 状态同步
}
```

**权限校验流程**：
1. 判断 Token 是否存在
2. 判断路由是否已加载
3. 校验用户权限码
4. 动态添加路由（如未添加）

### 7.5 菜单类型

| 类型 | 枚举值 | 说明 |
|-----|-------|-----|
| 目录 | `MenuType.目录` | 仅作为菜单容器，对应 LAYOUT 组件 |
| 菜单 | `MenuType.菜单` | 具体业务页面 |
| 按钮 | `MenuType.按钮` | 页面内操作按钮（权限控制粒度） |

### 7.6 路由元信息

```typescript
interface RouteMeta {
  title: string;          // 菜单标题
  icon?: string;          // 菜单图标
  hideMenu?: boolean;     // 隐藏菜单
  hideBreadcrumb?: boolean;// 隐藏面包屑
  keepAlive?: boolean;    // 是否缓存
  frameSrc?: string;      // iframe 嵌套地址
  currentActiveMenu?: string;// 当前激活菜单
  menuSort?: number;      // 菜单排序
}
```

---

## 8. API 接口设计

### 8.1 HTTP 封装

基于 Axios 封装统一请求方法：

```typescript
// packages/core/src/utils/http/axios/Axios.ts
export class VAxios {
  constructor(options: CreateAxiosOptions) {
    this.axiosInstance = axios.create(options);
    this.setupInterceptors();
  }

  get<T>(config, options?): Promise<T>
  post<T>(config, options?): Promise<T>
  put<T>(config, options?): Promise<T>
  delete<T>(config, options?): Promise<T>
  upload<T>(config, options?): Promise<T>    // 上传（超时30分钟）
  download<T>(config, options?): Promise<T>   // 下载（超时30分钟）
}
```

### 8.2 拦截器设计

#### 8.2.1 请求拦截器

```typescript
// 1. 添加取消重复请求
// 2. 添加 Token 到 Header
// 3. 处理 FormData 格式转换
// 4. 调用自定义请求前钩子
```

#### 8.2.2 响应拦截器

```typescript
// 1. 移除_pending_中的请求
// 2. 处理业务错误码
// 3. Blob/Stream 特殊处理
// 4. 统一错误处理
```

### 8.3 API 模块划分

```
@mfish/core/src/api/
├── sys/              # 系统管理 API
│   ├── User.ts       # 用户相关
│   ├── Menu.ts       # 菜单相关
│   ├── Role.ts       # 角色相关
│   ├── Org.ts        # 组织相关
│   ├── DictCategory.ts # 字典分类
│   ├── DictItem.ts   # 字典项
│   ├── DbConnect.ts  # 数据库连接
│   └── SsoTenant.ts  # 租户相关
├── storage/           # 文件存储 API
│   ├── SysFile.ts    # 文件管理
│   └── Upload.ts     # 上传接口
└── model/            # 数据模型
    └── BaseModel.ts  # 基础分页模型
```

### 8.4 核心 API 接口

#### 8.4.1 认证接口

| 接口 | 方法 | 说明 |
|-----|------|-----|
| `/oauth2/accessToken` | POST | 登录获取 Token |
| `/oauth2/user/info` | GET | 获取用户信息 |
| `/oauth2/user/revoke` | DELETE | 登出 |
| `/oauth2/refreshToken` | POST | 刷新 Token |
| `/oauth2/unLock` | POST | 解锁屏幕 |

#### 8.4.2 菜单接口

| 接口 | 方法 | 说明 |
|-----|------|-----|
| `/oauth2/menu/tree` | GET | 获取所有菜单树 |
| `/oauth2/menu/roleTree` | GET | 获取角色菜单树 |
| `/oauth2/menu/route` | GET | 获取菜单路由信息 |

### 8.5 请求配置

```typescript
interface RequestOptions {
  joinParamsToUrl?: boolean;    // 参数拼接到 URL
  formatDate?: boolean;         // 格式化日期
  joinTime?: boolean | string;  // 时间戳拼接
  ignoreCancelToken?: boolean;  // 忽略取消令牌
  errorMessageMode?: 'message' | 'modal' | 'none';
  successMessageMode?: 'message' | 'modal' | 'none';
  refreshToken?: boolean;        // 是否刷新 Token
  messageCode?: number[];       // 需要特殊处理的响应码
}
```

---

## 9. 性能优化策略

### 9.1 构建优化

#### 9.1.1 Vite 配置优化

```typescript
// internal/vite-config/src/index.ts
export function createViteConfig() {
  return {
    // 代码分割策略
    rollupOptions: {
      output: {
        manualChunks: {
          'echarts': ['echarts'],
          'vue-vendor': ['vue', 'vue-router', 'pinia']
        }
      }
    },
    // 依赖预构建
    optimizeDeps: {
      include: ['vue', 'vue-router', 'pinia', 'ant-design-vue']
    }
  };
}
```

#### 9.1.2 资源优化

- SVG 图标通过 `vite-plugin-svg-icons` 合并为一个 Sprite
- 图片资源通过 `vite-plugin-imagemin` 压缩
- 生产构建移除 console 和 debugger

### 9.2 运行时优化

#### 9.2.1 组件懒加载

```typescript
// 路由组件懒加载
const LoginRoute = {
  component: () => import('@/views/sys/login/Login.vue')
};

// 大屏组件按需加载
const MfChart = defineAsyncComponent(() => import('./charts/MfChart.vue'));
```

#### 9.2.2 KeepAlive 缓存

```typescript
// ProjectSetting.ts
openKeepAlive: true

// 路由 Meta
meta: { keepAlive: true }
```

#### 9.2.3 防抖节流

```typescript
// hooks/event/UseScrollTo.ts
// hooks/event/UseWindowSizeFn.ts
// directives/RepeatClick.ts
```

### 9.3 HTTP 优化

#### 9.3.1 请求缓存

```typescript
// 请求拦截器中处理
if (hasCache(options)) {
  return cacheRequest(cacheKey);
}
```

#### 9.3.2 请求取消

```typescript
// AxiosCanceler - 防止重复请求
class AxiosCanceler {
  addPending(config) { ... }
  removePending(config) { ... }
  removeAllPending() { ... }
}
```

### 9.4 内存优化

```typescript
// 组件卸载时清理
onUnmounted(() => {
  clearTimeout(timeId);
  clearInterval(intervalId);
  eventBus.off('event');
});
```

---

## 10. 安全策略

### 10.1 认证安全

#### 10.1.1 Token 管理

```typescript
// packages/core/src/utils/auth/index.ts
export function setAuthCache(key: string, value) {
  // 根据环境选择存储方式
  // 生产环境优先考虑内存缓存
}

// 刷新 Token 自动处理
// 401 响应时自动重试
```

#### 10.1.2 OAuth2 登录

支持授权码模式：

```typescript
// VITE_GLOB_LOGIN_TYPE = authorization_code
// VITE_GLOB_OAUTH2_URL = http://app.mfish.com.cn:11116/oauth2/authorize
```

### 10.2 前端安全

#### 10.2.1 XSS 防护

```typescript
// 模板中默认不渲染原始 HTML
<!-- 使用 v-html 需要谨慎 -->
<div v-html="safeContent"></div>
```

#### 10.2.2 CSRF 防护

```typescript
// 请求头中添加 CSRF Token
headers: {
  'X-CSRF-TOKEN': getCsrfToken()
}
```

### 10.3 权限控制

#### 10.3.1 菜单权限

```typescript
// 路由守卫中校验
function createPermissionGuard(router) {
  router.beforeEach(async (to) => {
    const permissionStore = usePermissionStoreWithOut();
    // 无 Token 重定向登录
    if (!permissionStore.getToken) {
      return redirectLogin();
    }
    // 校验权限码
    if (!hasPermission(to.meta.permissions)) {
      return redirect403();
    }
    return true;
  });
}
```

#### 10.3.2 按钮权限

```typescript
// v-permission 指令
const permissionStore = usePermissionStoreWithOut();
v-permission="['btn:add']"
```

### 10.4 数据安全

| 数据类型 | 处理方式 |
|---------|---------|
| 用户密码 | 前端不存储，HTTPS 传输 |
| Token | localStorage 存储，生产环境建议内存 |
| 敏感配置 | 环境变量管理，不提交代码仓库 |
| 错误日志 | 脱敏处理后再上报 |

---

## 11. 兼容性要求

### 11.1 浏览器兼容性

| 浏览器 | 最低版本 | 说明 |
|-------|---------|------|
| Chrome | 80+ | 推荐使用 |
| Firefox | 75+ | |
| Safari | 13+ | macOS 系统 |
| Edge | 80+ | |
| IE | 不支持 | 项目基于 ES2020 |

### 11.2 Node 版本要求

```json
{
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
```

### 11.3 响应式断点

```typescript
// packages/core/src/enums/BreakpointEnum.ts
enum BreakpointEnum {
  XS = 480,   // 超小屏幕 手机
  SM = 576,   // 小屏幕 手机
  MD = 768,   // 中等屏幕 平板
  LG = 992,   // 大屏幕 桌面
  XL = 1200,  // 超大屏幕 桌面
  XXL = 1600  // 超超大屏幕
}
```

### 11.4 Polyfill 策略

```typescript
// vite.config.ts
plugins: [
  legacy({
    targets: ['chrome80'],
    additionalLegacyPolyfills: ['regenerator-runtime']
  })
]
```

---

## 12. 开发规范

### 12.1 代码规范

#### 12.1.1 ESLint 配置

```javascript
// eslint.config.mjs
import { defineConfig } from '@mfish/eslint-config';
export default defineConfig();
```

**核心规则**：
- `@typescript-eslint/no-unused-vars`: 禁止未使用的变量
- `vue/multi-word-component-names`: 组件名必须多个单词
- `vue/no-setup-props-destructure`: setup 中不解构 props

#### 12.1.2 Prettier 配置

```javascript
// .prettierrc.mjs
import prettierConfig from '@mfish/prettier-config';
export default prettierConfig;
```

**格式化规则**：
- 单引号
- 不加分号
- 2 空格缩进
- 行宽 100

#### 12.1.3 Stylelint 配置

```javascript
// internal/stylelint-config/index.mjs
import stylelintConfig from '@mfish/stylelint-config';
export default stylelintConfig;
```

### 12.2 Git 规范

#### 12.2.1 提交信息格式

```
<type>(<scope>): <subject>

feat(user): add login by oauth2
fix(menu): fix route guard issue
chore(deps): upgrade vue to 3.5
docs(readme): update api documentation
```

#### 12.2.2 分支管理

```bash
main          # 主分支（生产环境）
develop       # 开发分支
feature/*     # 功能分支
bugfix/*      # 修复分支
release/*     # 发布分支
```

### 12.3 组件规范

#### 12.3.1 组件命名

```typescript
// PascalCase 命名
// 文件名与组件名一致
// Modal/index.ts -> Modal.vue
// Table/src/Table.vue -> export { Table }

// 目录结构
components/
├── Foo/
│   ├── index.ts       // 导出
│   └── Foo.vue        // 主组件
└── FooBar/
    ├── index.ts
    └── FooBar.vue
```

#### 12.3.2 Props 定义

```typescript
// 使用 TypeScript 类型定义
interface Props {
  title: string;
  count?: number;
  type: 'primary' | 'secondary';
  onClick?: (value: string) => void;
}

const props = withDefaults(defineProps<Props>(), {
  count: 0,
  type: 'primary'
});
```

### 12.4 目录规范

```bash
src/
├── api/              # API 接口
├── assets/           # 静态资源
├── components/       # 业务组件
│   ├── general/      # 通用组件
│   ├── nocode/       # 大屏组件
│   └── views/        # 页面组件
├── layouts/          # 布局组件
├── router/           # 路由配置
├── stores/           # Pinia Store
├── utils/            # 工具函数
├── views/            # 页面
├── App.vue
└── main.ts
```

---

## 13. 测试策略

### 13.1 测试范围

| 类型 | 覆盖目标 | 工具 |
|-----|---------|------|
| 单元测试 | 工具函数、hooks、store | Vitest + @vue/test-utils |
| 组件测试 | 业务组件行为 | @vue/test-utils |
| E2E 测试 | 关键业务流程 | Cypress / Playwright |

### 13.2 单元测试规范

```typescript
// __tests__/utils/date.spec.ts
import { describe, it, expect } from 'vitest';
import { formatDate } from '../date';

describe('formatDate', () => {
  it('should format date correctly', () => {
    expect(formatDate('2024-01-01')).toBe('2024-01-01');
  });
});
```

### 13.3 组件测试规范

```typescript
// __tests__/components/Button.spec.ts
import { mount } from '@vue/test-utils';
import Button from '../Button.vue';

describe('Button', () => {
  it('should emit click event', async () => {
    const wrapper = mount(Button, { props: { type: 'primary' } });
    await wrapper.trigger('click');
    expect(wrapper.emitted('click')).toBeTruthy();
  });
});
```

### 13.4 测试覆盖率要求

| 指标 | 目标 |
|-----|------|
| 分支覆盖率 | >= 70% |
| 函数覆盖率 | >= 80% |
| 行覆盖率 | >= 70% |

---

## 14. 项目目录结构

```
mfish-nocode-view/
├── packages/
│   ├── core/                      # @mfish/core 核心包
│   │   └── src/
│   │       ├── api/               # API 接口定义
│   │       │   ├── sys/            # 系统管理 API
│   │       │   │   ├── model/      # 数据模型
│   │       │   │   ├── User.ts
│   │       │   │   ├── Menu.ts
│   │       │   │   ├── Role.ts
│   │       │   │   └── ...
│   │       │   ├── storage/        # 存储 API
│   │       │   └── index.ts
│   │       ├── components/         # 基础组件
│   │       │   ├── Basic/
│   │       │   ├── Button/
│   │       │   ├── Form/
│   │       │   ├── Table/
│   │       │   ├── Modal/
│   │       │   ├── Drawer/
│   │       │   ├── Tree/
│   │       │   ├── Upload/
│   │       │   ├── Icon/
│   │       │   └── ...
│   │       ├── directives/         # 指令
│   │       │   ├── Permission.ts
│   │       │   ├── Loading.ts
│   │       │   ├── RepeatClick.ts
│   │       │   └── ripple/
│   │       ├── enums/              # 枚举定义
│   │       │   ├── AppEnum.ts
│   │       │   ├── CacheEnum.ts
│   │       │   ├── HttpEnum.ts
│   │       │   ├── MenuEnum.ts
│   │       │   └── ...
│   │       ├── hooks/              # 组合式函数
│   │       │   ├── core/           # 核心 hooks
│   │       │   ├── web/            # 业务 hooks
│   │       │   ├── setting/         # 配置 hooks
│   │       │   ├── event/          # 事件 hooks
│   │       │   └── component/       # 组件 hooks
│   │       ├── i18n/               # 国际化
│   │       │   ├── lang/
│   │       │   │   ├── zh-CN/
│   │       │   │   └── en/
│   │       │   ├── SetupI18n.ts
│   │       │   └── UseLocale.ts
│   │       ├── router/             # 路由
│   │       │   ├── routers/        # 路由定义
│   │       │   ├── guard/          # 路由守卫
│   │       │   ├── menus/          # 菜单处理
│   │       │   ├── helper/         # 路由辅助
│   │       │   ├── index.ts
│   │       │   ├── Types.ts
│   │       │   └── Constant.ts
│   │       ├── settings/           # 配置项
│   │       │   ├── DesignSetting.ts
│   │       │   ├── ProjectSetting.ts
│   │       │   ├── LoginSetting.ts
│   │       │   └── ...
│   │       ├── utils/              # 工具函数
│   │       │   ├── http/          # HTTP 相关
│   │       │   ├── cache/         # 缓存相关
│   │       │   ├── auth/          # 认证相关
│   │       │   ├── file/          # 文件相关
│   │       │   ├── event/         # 事件相关
│   │       │   └── ...
│   │       └── logics/            # 业务逻辑
│   │           ├── theme/        # 主题处理
│   │           ├── mitt/          # 事件总线
│   │           ├── error-handle/  # 错误处理
│   │           └── InitAppConfig.ts
│   │
│   ├── stores/                     # @mfish/stores 状态管理包
│   │   └── src/
│   │       ├── modules/            # Store 模块
│   │       │   ├── User.ts
│   │       │   ├── App.ts
│   │       │   ├── Permission.ts
│   │       │   ├── MultipleTab.ts
│   │       │   ├── Dict.ts
│   │       │   ├── TableSetting.ts
│   │       │   ├── I18n.ts
│   │       │   ├── ErrorLog.ts
│   │       │   └── index.ts
│   │       └── index.ts
│   │
│   ├── nocode/                     # @mfish/nocode 大屏组件包
│   │   └── core/                   # 组件配置
│   │       ├── MfBarLineConfig.js
│   │       ├── MfPieConfig.js
│   │       ├── MfTableConfig.js
│   │       ├── MfMapConfig.js
│   │       └── ... (50+ 组件配置)
│   │
│   └── custom-api/                 # @mfish/custom-api
│
├── internal/                        # 内部工具配置
│   ├── eslint-config/              # ESLint 配置
│   ├── prettier-config/            # Prettier 配置
│   ├── stylelint-config/           # Stylelint 配置
│   ├── ts-config/                  # TypeScript 配置
│   │   ├── base.json               # 基础配置
│   │   ├── vue-app.json            # Vue 应用配置
│   │   ├── library.json            # 库配置
│   │   └── ...
│   └── vite-config/                # Vite 配置
│
├── src/                            # 主应用
│   ├── views/                      # 页面
│   │   ├── sys/                    # 系统页面
│   │   │   ├── login/              # 登录页面
│   │   │   ├── account/            # 账户管理
│   │   │   ├── menu/               # 菜单管理
│   │   │   ├── role/               # 角色管理
│   │   │   ├── org/                # 组织管理
│   │   │   ├── dict/               # 字典管理
│   │   │   └── ...
│   │   ├── nocode/                 # 大屏页面
│   │   │   ├── mf-screen/          # 大屏设计
│   │   │   ├── screen-folder/      # 大屏文件夹
│   │   │   ├── screen-resource/    # 大屏资源
│   │   │   ├── mf-http/            # HTTP 配置
│   │   │   ├── mf-api/             # API 管理
│   │   │   └── ...
│   │   ├── workflow/               # 工作流
│   │   ├── dashboard/              # 仪表盘
│   │   └── demo/                   # 示例页面
│   │
│   ├── layouts/                    # 布局组件
│   │   ├── default/               # 默认布局
│   │   │   ├── header/
│   │   │   ├── sider/
│   │   │   ├── menu/
│   │   │   ├── tabs/
│   │   │   ├── content/
│   │   │   ├── footer/
│   │   │   ├── setting/
│   │   │   └── index.vue
│   │   ├── page/                  # 页面布局
│   │   └── iframe/                # Iframe 布局
│   │
│   ├── components/                # 业务组件
│   │   ├── general/               # 通用业务组件
│   │   │   ├── Application/       # 应用级组件
│   │   │   │   ├── AppSearch/
│   │   │   │   └── AppDarkModeToggle/
│   │   │   ├── SimpleMenu/        # 简单菜单
│   │   │   └── FlowChart/         # 流程图
│   │   └── nocode/                # 大屏组件
│   │       ├── screen/
│   │       └── common/
│   │
│   ├── router/                    # 路由
│   │   ├── index.ts
│   │   └── routers/               # 业务路由
│   │       └── index.ts
│   │
│   ├── App.vue                    # 根组件
│   ├── main.ts                    # 入口文件
│   └── _app.config.js             # 应用配置
│
├── .env                           # 环境变量
├── .env.development               # 开发环境变量
├── .env.production                # 生产环境变量
├── .env.test                      # 测试环境变量
├── index.html                     # HTML 入口
├── package.json                   # 项目配置
├── pnpm-workspace.yaml            # pnpm 工作空间配置
├── tsconfig.json                  # TypeScript 配置
├── vite.config.ts                 # Vite 配置
├── turbo.json                     # Turbo 配置
├── eslint.config.mjs              # ESLint 配置
├── .prettierrc.mjs               # Prettier 配置
├── Dockerfile                     # Docker 配置
├── docker-compose.yml             # Docker Compose 配置
└── README.md                      # 项目说明
```

---

## 15. 附录

### 15.1 环境变量说明

| 变量名 | 说明 | 示例值 |
|-------|------|-------|
| `VITE_USE_MOCK` | 是否启用 Mock | `false` |
| `VITE_PUBLIC_PATH` | 公共路径 | `/` |
| `VITE_PROXY` | 开发代理 | `["/api","http://localhost:8888"]` |
| `VITE_DROP_CONSOLE` | 移除 console | `false` |
| `VITE_GLOB_API_URL` | API 基础地址 | `/api` |
| `VITE_GLOB_LOGIN_TYPE` | 登录类型 | `password` / `authorization_code` |
| `VITE_GLOB_OAUTH2_URL` | OAuth2 地址 | `http://localhost:8888/oauth2/authorize` |

### 15.2 常用命令

```bash
# 开发
pnpm dev

# 构建
pnpm build

# 构建测试环境
pnpm build:test

# 类型检查
pnpm type:check

# 代码检查
pnpm lint

# 预览
pnpm preview
```

### 15.3 更新日志

项目采用语义化版本 `semver`：

- **主版本号**：不兼容的 API 修改
- **次版本号**：向后兼容的功能新增
- **修订号**：向后兼容的问题修复

当前版本：`2.3.0`

---

## 文档维护

| 版本 | 日期 | 修改内容 | 作者 |
|-----|------|---------|------|
| 1.0 | 2026-04-14 | 初始版本 | mfish |

---

*本文档为 mfish-nocode-view 项目开发指导文档，供前后端开发团队参考使用。*
